import searchingDemo from '!!./searching.ts?raw';
import { demo } from "../../misc";

export const searching = {
    ...demo("searching", "searching", searchingDemo, {}, 'Demonstration a few different search patterns where the resulting objects are isolated in the view.'),
};
